# v1.1.0 Acceptance Evidence

- **Executed:** 2026-07-31
- **Blueberry rename reverified:** 2026-08-01
- **Branch:** `feat/v1.1.0-codex-hooks`
- **Implementation commit:** `53a0eda`
- **Environment:** macOS 26.5.2, Apple Silicon arm64, Python 3.10.0, Electron 35.7.5
- **Decision:** Not yet accepted; the real Codex smoke-test gate remains open

## Evidence Summary

| Gate | Result | Evidence |
|---|---|---|
| Python normalization and privacy contract | Pass | 10 Python test methods pass |
| Event routing and HTTP boundary | Pass | 33 Node assertions pass |
| Pet unavailable degradation | Pass | 20 closed-pet Hook executions finish neutrally below the 500 ms ceiling |
| Fixture Hook → HTTP → router → IPC | Pass | All seven supported fixture events exit `0`, emit exactly `{}`, and reach the local event service |
| Visual state rendering | Pass | DOM state and same-moment screenshots confirm Idle, Thinking, Working, Attention, Happy, and Sleeping |
| Fixture Hook → visible animation | Pass | `Stop` produces Happy and `SessionEnd` produces Sleeping through the Python Hook |
| Hook process latency with pet running | Pass | Seven-event median 37.41 ms; maximum 125.98 ms |
| Apple Silicon `.app` | Pass | Version 1.1.0, Mach-O arm64 |
| Packaged `.app` runtime smoke | Pass | App launches, binds loopback port, accepts Python Hook and manual state |
| Local read-only DMG | Pass with workaround | `hdiutil` produced a compressed UDZO image |
| Standard `electron-builder` DMG | Pass | 101 MB compressed read-only arm64 DMG and blockmap produced |
| Renamed Blueberry arm64 package | Pass | Blueberry bundle identity, DMG, checksum, and packaged runtime smoke verified |
| Real Codex task smoke test | Pending | `~/.codex/hooks.json` is absent; install, `/hooks` trust review, and fresh-task verification remain |

## Automated Verification

The repository test command covers:

- seven supported Codex Hook mappings;
- ignored compact and recursive Stop cases;
- stable event IDs;
- privacy allowlisting;
- malformed and unknown input;
- loopback delivery and neutral failure;
- protocol and metadata validation;
- semantic event routing;
- request method, path, body-size, duplicate, and error handling;
- Electron main/preload/renderer wiring.

At the time of this evidence record:

```text
Node:   33 assertions passed
Python: 10 test methods passed
```

The final verification command must be rerun immediately before branch completion; this document records the acceptance evidence, not a substitute for the test output.

## Visual Acceptance

The Electron renderer was inspected through its development debugging interface. Each state was accepted only when both conditions were true:

1. the local endpoint returned the expected state;
2. the renderer DOM exposed the expected visible GIF and state class at the same moment as the screenshot.

Observed results:

| State | Renderer evidence | User-visible behavior |
|---|---|---|
| `idle` | base pet visible; no state class | normal pet |
| `thinking` | `think-img: block`; class `thinking` | question-mark thinking animation |
| `working` | `work-img: block`; class `working` | keyboard work animation |
| `attention` | `attention-img: block`; class `attention` | question-mark pulse; intentionally reuses thinking art |
| `happy` | `happy-img: block`; class `happy` | sparkle completion animation |
| `sleeping` | `sleep-img: block`; class `sleeping` | eyes-closed sleep animation |

The first operating-system window screenshots looked like Idle even after HTTP `200`. Investigation showed that transparent-window capture and short 3–5 second animations made those screenshots unreliable. The gate was therefore not lowered: inspection moved closer to the renderer and paired DOM evidence with a same-moment image.

## Fixture-Driven End-to-End Check

The seven official-shape fixtures were sent through the real Python process while Electron was listening on `127.0.0.1:18920`.

```text
session-start.json:          125.98 ms
user-prompt-submit.json:      37.41 ms
pre-tool-use.json:            37.23 ms
post-tool-use.json:           35.00 ms
permission-request.json:      35.51 ms
stop.json:                    37.66 ms
session-end.json:             45.06 ms
median:                       37.41 ms
maximum:                     125.98 ms
```

Every invocation exited `0`, wrote exactly `{}` to stdout, and left stderr empty. A same-call renderer inspection additionally confirmed:

- `stop.json` → `happy-img: block` and class `happy`;
- `session-end.json` → `sleep-img: block` and class `sleeping`.

These measurements cover Python process startup through local HTTP response and IPC delivery. They do not claim a scientifically measured photon-to-eye latency; the visual state was verified separately.

## Packaging Evidence

The standard arm64 build produced:

```text
dist/mac-arm64/WorkBuddy Pet.app
CFBundleShortVersionString: 1.1.0
Executable: Mach-O 64-bit arm64
```

The original 160×160 runtime pixel asset did not satisfy the builder's 512×512 icon requirement. A separate 512×512 transparent package icon was added while preserving the runtime sprite.

An early DMG helper download timed out against the GitHub CDN. A local fallback artifact was created at that point:

```text
dist/WorkBuddy Pet-1.1.0-arm64-local.dmg
Format: compressed read-only UDIF (UDZO)
Compressed bytes in use: 115,821,840
```

The local DMG proves that the arm64 application can be packaged into a mountable read-only image. It does not prove that the configured `electron-builder` DMG job passed.

After restoring the build runtime's npm executable path and retrying when the external download recovered, the standard command completed and produced:

```text
dist/WorkBuddy Pet-1.1.0-arm64.dmg
Size: 101 MB
Format: compressed read-only UDIF
SHA-256: f23d1394b3b6a97a41745d74e0a7af085a8bce73cbf3698881dd629a1cbe48be
```

The successful standard artifact supersedes the local fallback for release testing. The timeout and fallback remain recorded because they demonstrate the difference between an external build interruption and a product-code failure.

The packaged `.app` executable was then launched directly. It bound `127.0.0.1:18920`, accepted `stop.json` through the Python Hook with neutral `{}` output, accepted a manual `working` state with HTTP `200`, and remained alive. The Mac was locked during this final runtime smoke, so no packaged-window screenshot was claimed; the six-state visual evidence was collected separately from the development build.

## Blueberry Rename and Package Reverification

The product-identity migration was verified on 2026-08-01 after the active package, runtime interfaces, current documentation, and external notes were renamed to Blueberry.

### Automated results

```text
Node:   36 assertions passed, including 3 Blueberry naming assertions
Python: 10 test methods passed
Focused privacy and closed-pet degradation: 2 tests passed
```

The naming test was observed failing before the migration and passing afterward. The privacy test again confirmed that prompt, command, path, transcript, tool input/output, and final-answer content do not enter the canonical event. The closed-pet test again completed 20 neutral Hook invocations below the existing 500 ms ceiling.

### Build-environment diagnosis

The first renamed build attempt stopped before packaging because the Codex bundled Node runtime did not include an `npm` executable, while electron-builder selected npm from the repository's existing `package-lock.json`. No product code failed.

For the verified build:

- the official Node.js 24.14.0 macOS arm64 archive was downloaded into a temporary directory;
- its SHA-256 matched the official `SHASUMS256.txt` value `a1a54f46a750d2523d628d924aab61758a51c9dad3e0238beb14141be9615dd3`;
- the included Node `v24.14.0` and npm `11.9.0` were used only as the build process PATH;
- a stalled GitHub checksum request was avoided by passing electron-builder the existing Electron 35.7.5 arm64 cache zip;
- the cache zip passed `unzip -t` and had SHA-256 `2fe3a3cfad607a8c1627f6f2bb9834f959c665ef575b663206db11929634b92f`;
- `package.json`, package-manager choice, and application runtime code were not changed to work around the environment.

The standard electron-builder job then exited `0` and produced:

```text
dist/mac-arm64/Blueberry.app
Bundle ID: com.blueberry.pet
Version: 1.1.0
Executable: Mach-O 64-bit arm64

dist/Blueberry-1.1.0-arm64.dmg
Size: 105,843,453 bytes (101 MB displayed)
Format: UDZO compressed read-only UDIF
SHA-256: b56f9059c2e63d1847d1839301cf402cb5543e1f51d0a63fd4292ceff207d63d

dist/Blueberry-1.1.0-arm64.dmg.blockmap
Displayed size: 109 KB
```

Code signing remained skipped because the machine has no valid Developer ID Application identity. Signing, notarization, and public distribution remain outside the v1.1.0 MVP.

### Packaged runtime smoke

The renamed packaged executable was launched directly and observed listening on `127.0.0.1:18920`. It accepted the real Python adapter with `stop.json`, for which the Hook wrote exactly `{}` and no stderr, and accepted a manual `working` state with HTTP `200`. The test process then exited normally and released port 18920.

This proves the renamed packaged application, Python adapter, loopback service, and event route operate together. It still does not substitute for installing and trusting the Hook in the real Codex configuration.

## Real Codex Acceptance — 2026-08-01

The release candidate was installed into the real user environment:

- `/Applications/Blueberry.app` contains version `1.1.0`, bundle ID `com.blueberry.pet`, and an arm64 executable;
- the stable Python adapter is installed at `~/Library/Application Support/Blueberry/hooks/codex_hook.py`;
- `~/.codex/hooks.json` contains the seven v1.1.0 lifecycle handlers;
- `/hooks` reported all seven handlers as installed, active, and trusted.

Fresh Codex CLI sessions then exercised the installed configuration. A temporary diagnostic recorded only Hook names and normalization booleans—never prompts, commands, paths, transcripts, tool inputs, tool outputs, or assistant messages—and was removed after the run. It confirmed successful normalization of `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`, and `SessionEnd`. The separately triggered genuine `PermissionRequest` was verified through the visible Attention state.

Visible operating-system inspection confirmed:

- `UserPromptSubmit` displayed the Thinking image;
- a real shell tool call displayed the Working image and keyboard;
- a genuine read-only-sandbox escalation displayed the Attention image;
- the escalation was canceled, and `/tmp/blueberry-permission-smoke` was not created;
- session close returned Blueberry to Sleeping.

The real `Stop` followed immediately by `SessionEnd` uncovered a state-priority defect: Happy suppressed Sleeping and the session ended in Idle. A failing regression test was added first. The renderer now defers Sleeping until Happy finishes, while new activity cancels a stale deferred sleep request. The focused policy tests passed `3/3`; the complete post-fix regression suite passed `39/39` Node tests and `10/10` Python tests. The corrected arm64 package was rebuilt, installed, and confirmed to contain `src/renderer/completion-state-policy.js`.

The final corrected DMG is a UDZO compressed read-only image, `105,843,927` bytes, with SHA-256 `7a058c0cf42a53b23be5c96d65ddef0368461e375f8e8aa094e211f8a951fa8d`.

This accepts the v1.1.0 minimum Codex-to-Blueberry reaction loop. It does not claim public distribution readiness.

## Remaining Release Risks

1. The macOS application is unsigned and unnotarized; public distribution is outside v1.1.0.
2. Intel macOS is untested and outside v1.1.0.
3. Review the 17 high-severity findings reported in the inherited Electron build dependency tree; do not use a blind forced upgrade.
4. Repeated permission reminders, finer tool-specific reactions, compact events, and subagent behavior remain v1.2.0 work.
