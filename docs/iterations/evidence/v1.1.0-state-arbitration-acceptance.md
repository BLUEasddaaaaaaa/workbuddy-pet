# v1.1.0 State Arbitration Acceptance Evidence

> **Historical evidence notice (2026-08-02):** This result was superseded after later runtime checks exposed unresolved final-session fallback behavior and an invalid `Stop -> Happy` product assumption. The implementation covered here has been withdrawn and this file must not be used as evidence that v1.1.0 is currently accepted or release-ready.

- **Date:** 2026-08-01
- **Result:** SUPERSEDED — later acceptance failed
- **Release recommendation:** Not release-ready; retained as historical evidence only
- **Test target:** Apple Silicon macOS, installed `/Applications/Blueberry.app`

## Scope and Decision

This acceptance checks the lightweight session-aware coordinator, renderer deduplication, renderer-readiness delivery, arm64 packaging, and the installed Codex Hook path. Earlier runs first found a real stale visual-ledger defect and then failed to obtain reliable visual evidence through Computer Use. A final clean-room package run proved that the latter blocker was a stale-package/observation problem rather than a remaining renderer defect: package contents were byte-checked before installation and CDP directly observed the required repeated Attention sequence.

No production code was changed during this acceptance task.

## Runtime Paths

- Node: `/Users/molan/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node` (`v24.14.0`)
- npm: `/Applications/ChatGPT.app/Contents/Resources/cua_node/bin/npm` (`11.9.0`)
- Python: `/usr/bin/python3` (`3.9.6`)
- Installed Hook: `~/Library/Application Support/Blueberry/hooks/codex_hook.py`
- Installed app: `/Applications/Blueberry.app`

The installed Hook was byte-for-byte identical to the repository adapter (`cmp` exit 0).

## Automated Acceptance

The shell initially had no `npm` command. The complete suites were therefore run with the bundled Electron binary in Node mode and system Python, followed by the focused suites. A later build used the discovered real bundled Node/npm paths.

```bash
ELECTRON_RUN_AS_NODE=1 node_modules/.pnpm/electron@35.7.5/node_modules/electron/dist/Electron.app/Contents/MacOS/Electron --test tests/js/*.test.js
/usr/bin/python3 -m unittest discover -s tests/python -p 'test_*.py' -v
```

- JavaScript: **77/77 passed**, 0 failed, 0 cancelled, 0 skipped, 0 todo; 277.456 ms.
- Python: **10/10 passed**, 0 failed; 2.345 s.
- No unhandled rejection, timer open-handle warning, or test warning was emitted.

Focused command:

```bash
ELECTRON_RUN_AS_NODE=1 node_modules/.pnpm/electron@35.7.5/node_modules/electron/dist/Electron.app/Contents/MacOS/Electron --test tests/js/state-coordinator.test.js tests/js/external-state-policy.test.js tests/js/renderer-state-bridge.test.js tests/js/electron-wiring.test.js
```

- Focused result: **40/40 passed**, 0 failures; 141.391 ms.

## Quantified Policy Metrics

These results come from deterministic fake-clock policy tests, not visual inference.

| Metric | Requirement | Evidence | Result |
|---|---:|---|---|
| Same-state extra emissions | 0 | repeated Working emitted once and did not restart its hold | PASS |
| Thinking minimum display | 1000 ms | Working remained pending until the fake clock reached 1000 ms | PASS |
| Working minimum display | 1000 ms | exported policy constant and hold behavior verified | PASS |
| Attention continuity | at least 3000 ms and until session advances/ends | 3000 ms hold plus cross-session Working non-interruption tests | PASS |
| Happy continuity | 3000 ms unless Attention | 3000 ms non-urgent hold and immediate Attention interruption tests | PASS |
| End one of two sessions | reveal remaining state | remaining session state emitted | PASS |
| End final session | Sleeping | Sleeping emitted | PASS |
| Stale active recovery | 30000 ms | Thinking and Working removed and Idle emitted at 30000 ms | PASS |
| Renderer readiness | buffer/replay | pre-attach latest state replayed exactly once; detach/reattach tested | PASS |

The readiness bridge was added after review identified a race between starting the event server and attaching the renderer IPC delivery function. It buffers only the latest approved state until the renderer is ready; it does not choose state priority or deduplicate presentation.

## Build, Package, and Install

Build command:

```bash
PATH="/Users/molan/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/Applications/ChatGPT.app/Contents/Resources/cua_node/bin:$PATH" npm run build:mac
```

`electron-builder` 26.15.3 exited 0 and generated:

- `dist/mac-arm64/Blueberry.app`
- `dist/Blueberry-1.1.0-arm64.dmg` (approximately 101 MiB)
- `dist/Blueberry-1.1.0-arm64.dmg.blockmap` (approximately 109 KiB)

Signing was skipped because no valid Developer ID identity was installed. The executable has only an ad-hoc/linker signature, is not notarized, and `spctl` does not accept it for public distribution. This remains outside the local MVP scope but must be disclosed.

The previous exact `/Applications/Blueberry.app` was stopped and moved to `/tmp/blueberry-installed-backup.8913JQ/Blueberry.app`; the rebuilt app replaced it. The installed `app.asar` contains:

- `/src/main/state-coordinator.js`
- `/src/main/renderer-state-bridge.js`
- `/src/renderer/external-state-policy.js`

## Installed Adapter Rounds

Every installed Hook process exited 0, wrote `{}` to stdout, and wrote nothing to stderr.

| Round | Events | Minimum | Mean | Maximum | Warm-path target |
|---|---:|---:|---:|---:|---|
| Single-session lifecycle | 9 | 49.57 ms | 75.63 ms | 107.74 ms | PASS (<200 ms) |
| Permission setup | 4 | 40.31 ms | 48.81 ms | 62.43 ms | PASS (<200 ms) |
| Overlapping two-session fixture | 9 | 56.84 ms | 84.41 ms | 120.10 ms | PASS (<200 ms) |

The single-session sequence covered SessionStart, UserPromptSubmit, repeated Pre/PostToolUse, PermissionRequest, Stop, and SessionEnd. The overlap sequence created sessions A and B, ended A while B remained active, advanced B, then ended B.

Computer Use observation itself can generate Codex tool Hooks, so final Sleeping and cross-session visual attribution are not reliable from screenshots alone. Those exact arbitration metrics are accepted only from deterministic tests. They must not be presented as visually proven.

## Blocking Reproduction

1. A unique canonical Permission event was posted to the installed app:

```text
POST /event -> HTTP 200 in 14.655 ms
{"status":"ok","event_id":"evt_acceptdirect001","state":"attention"}
```

2. The immediate installed-app screenshot showed the Idle sprite instead of Attention.
3. A direct diagnostic `POST /state {"state":"attention"}` returned HTTP 200 and produced the question-mark Attention sprite once.
4. After the renderer's five-second Attention timeout restored the visual to Idle, another `POST /state {"state":"attention"}` again returned HTTP 200 but remained visually Idle.

The renderer presentation policy remembers the last externally approved state, while renderer-owned timeouts can change the visible state without updating that policy. It can therefore remember `attention` while the user sees Idle and suppress the next legitimate `attention` as a duplicate. This invalidates the required guarantee that Permission feedback is observable.

## Acceptance Matrix

| Area | Result | Notes |
|---|---|---|
| Complete JS/Python suites | PASS | 87 total tests, zero failures |
| Coordinator metrics | PASS | deterministic fake-clock evidence |
| Renderer readiness bridge | PASS | buffered/replayed latest state |
| arm64 build | PASS | unsigned and unnotarized |
| Install contents | PASS | all three state modules present |
| Hook latency | PASS | all 22 measured processes below 200 ms |
| Installed Attention observability | **FAIL** | stale renderer-policy state suppresses a legitimate repeat |
| Installed overlap visual attribution | INCONCLUSIVE | Computer Use observer Hooks interfere; deterministic tests pass |

## Fresh Re-acceptance After the Two-phase Visual Ledger Fix

The renderer fix was re-accepted independently at commit `ef95df5` on 2026-08-01. This run did not reuse the earlier automated or visual result.

### Fresh automated and package evidence

- Bundled Node `v24.14.0` and npm `11.9.0` were used.
- Complete JavaScript suite: **80/80 passed**, zero failures, 190.834 ms.
- Complete Python suite: **10/10 passed**, zero failures, 2.458 s.
- Focused coordinator, external-policy, readiness-bridge, and Electron-wiring suite: **43/43 passed**, zero failures, 85.475 ms.
- `npm run build:mac` produced `dist/mac-arm64/Blueberry.app`, a 105,761,994-byte DMG, and a 111,314-byte blockmap.
- The packaged `app.asar` contains the state coordinator, renderer-readiness bridge, external-state policy, renderer, and renderer HTML.
- The app remains ad-hoc signed and fails `spctl`; this is unchanged and prevents public distribution.

The previous exact installed app was stopped and moved to `/tmp/blueberry-installed-backup.sA7W2S/Blueberry.app`. The rebuilt app was copied to `/Applications/Blueberry.app`, launched, and returned HTTP 200 on readiness attempt 14 (approximately 3.5 seconds).

### Mandatory original-symptom gate

The original symptom was tested before canonical or multi-session traffic:

1. `POST /state {"state":"attention"}` returned HTTP 200 in **15.930 ms**.
2. Computer Use still showed the Idle sprite instead of the expected Attention visual.
3. Because Computer Use itself can produce Codex Hook traffic, one permitted isolation attempt temporarily moved the installed adapter out of its configured path.
4. With the adapter isolated, the same POST returned HTTP 200 in **4.194 ms**, but Computer Use again showed Idle.
5. The adapter was immediately restored and is byte-for-byte identical to the repository adapter (`cmp` exit 0).

The acceptance gate therefore did not obtain a positive first-Attention observation. The required wait-to-Idle and second Attention replay, Happy replay/sound check, canonical Permission event, two adapter rounds, and overlapping-session runtime sequence were intentionally not run. Continuing would have produced downstream measurements without first proving the user-visible blocker was resolved.

The two-phase actual-visual ledger and renderer-readiness bridge are covered by deterministic tests, but this run did not visually capture the first diagnostic Attention state. Computer Use has both observer-Hook and capture-timing limitations, so this observation does **not** conclusively prove that the Task 3b product fix failed. It does mean the mandatory installed visual proof is absent. A separate debugging task must distinguish IPC delivery, renderer transition execution, asset visibility, capture timing, and other installed-runtime conditions.

## Final Fresh-Package Re-acceptance

The historical failures above are retained because they explain both the original defect and why package provenance became a mandatory gate. The final run used worktree `v1.1.0-codex-hooks` at `9ce74ee` (production implementation `ef95df5`) and did not change product code.

### Clean build and artifact identity

- The pre-existing worktree `dist` was moved, not deleted, to `/tmp/blueberry-dist-backup.s7PmgC/dist`.
- Fresh complete suites passed: **80/80 JavaScript** and **10/10 Python**, zero failures.
- `npm run build:mac` exited 0 and produced the arm64 app, approximately 101 MiB DMG, and 109 KiB blockmap.
- Before installation, `app.asar` was extracted to `/tmp/blueberry-packaged-asar.ZPI3Kz`.
- `renderer.js`, `external-state-policy.js`, `state-coordinator.js`, `renderer-state-bridge.js`, and `main.js` were each byte-for-byte identical to the worktree.
- The policy probe remains pure, renderer-owned restoration marks `idle`, and successful transitions centrally mark their actual visual state.
- The prior installed app was moved to `/tmp/blueberry-installed-backup.Y7IYKu/Blueberry.app`.
- After copying the fresh app, the installed `app.asar` was extracted to `/tmp/blueberry-installed-asar.eVchi5`; the same five-file byte comparison passed again.

### Authoritative installed-renderer gate

The installed Hook adapter was temporarily moved aside so activity generated by this acceptance could not alter the visual result. Its SHA-256 before and after restoration was identical: `9526ef347e6a22c2ee1d50480a9cca4f2f42fddf698c74e36190596c5e2c61b9`.

CDP read the installed renderer DOM directly and an independent IPC listener recorded every delivered `trigger-state` value:

| Step | HTTP | Direct DOM result |
|---|---:|---|
| diagnostic Idle | 4.799 ms | `pet-img`, no action class |
| first diagnostic Attention | 2.071 ms | `attention-img`, `attention` class |
| 5.5 seconds later | — | returned to `pet-img` Idle |
| second diagnostic Attention | 7.493 ms | Attention immediately and again at +0.5 seconds |
| unique canonical Permission | 3.837 ms | `attention-img`, `attention` class |

The independent IPC sequence was exactly `idle, attention, attention, attention`. This proves the previously failing `Attention → Idle → Attention` replay and the canonical Permission path without relying on Computer Use screenshots.

### Installed adapter measurements

Every process exited 0, wrote exactly `{}` to stdout, and wrote no stderr.

| Scenario | Events | Minimum | Mean | Maximum | Result |
|---|---:|---:|---:|---:|---|
| single session | 7 | 36.862 ms | 46.471 ms | 87.203 ms | PASS, all below 200 ms |
| overlapping sessions A/B | 9 | 36.606 ms | 37.074 ms | 37.447 ms | PASS, all below 200 ms |

Cross-session priority and hold boundaries remain deterministically covered by the coordinator tests. CDP is authoritative for installed presentation; Computer Use is intentionally not required because its own Codex tool activity can generate observer Hooks.

## Final Verdict

**PASS.** The clean build, complete tests, pre-install and post-install byte-identity gates, installed CDP sequence, canonical Permission event, adapter restoration, and adapter latency targets all passed. Blueberry v1.1.0 interaction stabilization is accepted for local macOS use. The app remains unsigned and unnotarized, so public distribution is still not recommended until the normal Apple signing/notarization work is completed.
