# v1.1.0 Renderer Controller Worktree Acceptance

- **Executed:** 2026-08-08
- **Result:** PASS for the simulated worktree runtime gate
- **Branch:** `feat/v1.1.0-codex-hooks`
- **Source HEAD:** `3253e6e28eff57eda53efad20c7499526a9d9835`
- **Worktree:** `/Users/molan/Documents/Codex/2026-07-30/zhe-ge/outputs/workbuddy/workbuddy-pet/.worktrees/v1.1.0-codex-hooks`
- **Runtime:** Electron 35.7.5 on Apple Silicon macOS

This run exercises the current worktree directly. It did not build or install an application, replace `/Applications/Blueberry.app`, change the real Codex configuration, or publish anything. No production source or test file changed during acceptance.

## Preconditions and automated evidence

Before launch, `git status --short` contained only the pre-existing, user-owned untracked `.superpowers/` directory. Ports `127.0.0.1:18920` and `127.0.0.1:19222` had no listener.

The fresh complete-suite command was:

```bash
PATH="/Applications/ChatGPT.app/Contents/Resources/cua_node/bin:/Users/molan/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin:$PATH" npm test
```

- JavaScript: **74/74 passed**, 0 failed, 0 cancelled, 0 skipped, 0 todo; 149.163 ms.
- Python: **10/10 passed**, 0 failed; 2.235 s.

The JavaScript fake-clock suite is the exact millisecond-boundary evidence. The worktree/CDP run below independently proves the real Renderer never changed early and that every new hold was measured from actual visual application.

## Launches, provenance, and observability gate

Default mode was launched first:

```bash
PATH="/Applications/ChatGPT.app/Contents/Resources/cua_node/bin:$PATH" npm start -- --remote-debugging-port=19222
```

CDP reported the Renderer URL as this worktree's `src/renderer/index.html`. Electron PID `64063` owned both `127.0.0.1:18920` and `127.0.0.1:19222`. `typeof window.__blueberryDebug` was `undefined`. The process was stopped with SIGINT and both ports were confirmed released.

Acceptance mode was then launched from the same worktree:

```bash
BLUEBERRY_ACCEPTANCE=1 PATH="/Applications/ChatGPT.app/Contents/Resources/cua_node/bin:$PATH" npm start -- --remote-debugging-port=19222
```

Electron PID `64297` owned both listeners during the run. CDP observed:

| Check | Observed | Result |
|---|---|---|
| Renderer provenance | worktree `src/renderer/index.html` URL | PASS |
| property writable | `false` | PASS |
| property configurable | `false` | PASS |
| property enumerable | `false` | PASS |
| facade frozen | `Object.isFrozen(...) === true` | PASS |
| facade keys | exactly `snapshot` | PASS |
| snapshot | callable and returned the five controller fields | PASS |

The initial snapshot was allowed to reflect activity from the live Codex observer. Each controlled round therefore began with the real `SessionStart` fixture, which established Idle before the measured sequence.

## Fixture method and delivery

Every event used the repository adapter and repository fixture, for example:

```bash
PYTHON_BIN="/Users/molan/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3"
"$PYTHON_BIN" hooks/codex_hook.py < tests/fixtures/codex/session-start.json
```

The exact order in each of two rounds was:

```text
session-start → user-prompt-submit → pre-tool-use → post-tool-use
→ permission-request → stop → session-end
```

All 14 required adapter processes exited `0`, wrote exactly `{}` to stdout, and wrote nothing to stderr. Their end-to-end process measurements were:

| Round | SessionStart | Prompt | PreTool | PostTool | Permission | Stop | SessionEnd |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 141.812 ms | 70.458 ms | 49.688 ms | 45.623 ms | 49.042 ms | 64.020 ms | 50.249 ms |
| 2 | 402.971 ms | 68.774 ms | 831.057 ms | 372.390 ms | 102.546 ms | 251.271 ms | 78.465 ms |

An additional real `user-prompt-submit.json` used for the sleeping wake gate exited `0`, wrote `{}`, left stderr empty, and took 168.141 ms. These process/HTTP results prove delivery only, not presentation timing.

The temporary `/tmp` harness initially rejected a successful SessionStart because it incorrectly expected a trailing newline after `{}`. That precheck had exit `0`, stdout `{}`, and empty stderr. Only the `/tmp` assertion was corrected; the complete two-round run above then restarted from the beginning. No repository file was changed for the harness correction.

## Reproduction procedure

Run these commands from the recorded worktree. Port checks must show no existing listener before launch; a conflict is a blocker and must not be killed as part of this procedure.

```bash
cd /Users/molan/Documents/Codex/2026-07-30/zhe-ge/outputs/workbuddy/workbuddy-pet/.worktrees/v1.1.0-codex-hooks
/usr/sbin/lsof -nP -iTCP:18920 -sTCP:LISTEN
/usr/sbin/lsof -nP -iTCP:19222 -sTCP:LISTEN
BLUEBERRY_ACCEPTANCE=1 PATH="/Applications/ChatGPT.app/Contents/Resources/cua_node/bin:$PATH" npm start -- --remote-debugging-port=19222
```

In a second shell, discover the Renderer target rather than assuming a page ID:

```bash
curl -sS http://127.0.0.1:19222/json/list
```

Select the entry whose `type` is `page` and whose URL is the recorded worktree's `src/renderer/index.html`, then connect a CDP client to its `webSocketDebuggerUrl`. Send `Runtime.evaluate` with `returnByValue: true`. The exact facade expression is:

```js
(() => {
  const d = Object.getOwnPropertyDescriptor(window, '__blueberryDebug');
  return {
    type: typeof window.__blueberryDebug,
    writable: d.writable,
    configurable: d.configurable,
    enumerable: d.enumerable,
    frozen: Object.isFrozen(window.__blueberryDebug),
    keys: Object.keys(window.__blueberryDebug),
    snapshotType: typeof window.__blueberryDebug.snapshot,
    snapshot: window.__blueberryDebug.snapshot(),
    href: location.href,
  };
})()
```

For every state/timing observation, use one `Runtime.evaluate` call with this expression so time, controller state, class, and image visibility are from the same Renderer turn:

```js
(() => {
  const snapshot = window.__blueberryDebug.snapshot();
  const ids = [
    'pet-img', 'read-img', 'think-img', 'work-img',
    'attention-img', 'happy-img', 'sleep-img', 'pet-shadow',
  ];
  const images = {};
  for (const id of ids) {
    const element = document.getElementById(id);
    const display = element ? getComputedStyle(element).display : null;
    images[id] = element ? {
      inline: element.style.display,
      computed: display,
      visible: display !== 'none',
    } : null;
  }
  return {
    now: performance.now(),
    snapshot,
    classes: [...document.getElementById('pet-container').classList],
    images,
    href: location.href,
  };
})()
```

Invoke the real adapter twice in this fixed order, recording monotonic process start/end time, exit code, stdout, and stderr for each invocation:

```bash
PYTHON_BIN="/Users/molan/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3"
for round in 1 2; do
  for fixture in \
    session-start user-prompt-submit pre-tool-use post-tool-use \
    permission-request stop session-end
  do
    "$PYTHON_BIN" hooks/codex_hook.py < "tests/fixtures/codex/$fixture.json"
  done
done
```

The sampling client must interleave those invocations with atomic CDP reads rather than run the shell loop without observations: sample Thinking after Prompt; send PreToolUse during its hold; sample just before and after Thinking's absolute deadline; sample Working before and after PostToolUse; send Permission during Working's hold and sample its deadline; send Stop and SessionEnd during Attention; then sample the Attention and Happy deadlines and the final Idle. Derive every requested target from the live snapshot (`protectedUntil - 1` and `protectedUntil + 1`), and use `Runtime.evaluate` with `awaitPromise: true` for a Renderer-side promise that waits until the target before evaluating the atomic expression.

The deadline wait expression used this shape; substitute the numeric target obtained from the preceding snapshot and use the full atomic body above for `takeAtomicSample`:

```js
new Promise((resolve) => {
  const target = 171286.4; // recorded protectedUntil - 1; do not hard-code across runs
  const wait = () => {
    const now = performance.now();
    if (now < target) {
      setTimeout(wait, Math.max(0, target - now));
      return;
    }
    resolve(takeAtomicSample());
  };
  wait();
})
```

Here `takeAtomicSample()` denotes the immediately preceding atomic IIFE with no independent state read before or after it; in a raw CDP request the harness embedded that IIFE directly in place of the function call. The corresponding post-deadline request used the same expression with `protectedUntil + 1`.

macOS wall-clock/CDP scheduling cannot guarantee that a requested `deadline - 1 ms` or `deadline + 1 ms` callback actually resumes at that exact millisecond. Record both the requested target and returned `performance.now()`; never relabel a late sample as an exact `+1999/+2000` capture. The deterministic fake-clock tests are authoritative for the exact millisecond boundary. Runtime CDP evidence is authoritative only for no early transition, the actual `visibleSince`/`protectedUntil` interval, state/DOM agreement, pending behavior, and eventual post-deadline transition.

## Atomic CDP observations

Every timing sample was one `Runtime.evaluate` expression returning `performance.now()`, `window.__blueberryDebug.snapshot()`, the pet classes, and computed/inline image visibility together.

| Scenario | Expected | Observed | Result |
|---|---|---|---|
| Thinking minimum | 2000 ms | both rounds had `protectedUntil - visibleSince = 2000`; Working remained pending through the deadline-edge sample | PASS |
| Working minimum | 1000 ms | both rounds had an exact 1000 ms deadline; Attention remained pending and Working image/class remained visible at the edge | PASS |
| PostToolUse duplicate | no restart/extension | round 1 stayed `171290/172290`; round 2 stayed `182201.4/183201.4` for `visibleSince/protectedUntil` | PASS |
| higher priority during protection | pending only | Attention stayed pending behind Working; visible DOM stayed Working | PASS |
| one-shot during protection | pending only | Happy stayed pending behind Attention; visible DOM stayed Attention | PASS |
| Attention actual hold | full 2000 ms from actual display | `172290.5→174290.5` and `183202.6→185202.6` | PASS |
| Happy actual hold | full 2000 ms from actual display | `177681.6→179681.6` and `185248.4→187248.4` | PASS |
| latest logical return | SessionEnd makes logical Idle; one-shots then return Idle | both rounds showed Attention, then Happy, then Idle | PASS |
| stale replay | no old Working/Attention after return | no stale visual replay in either round | PASS |
| DOM agreement | snapshot state matches class/image | every critical sample matched Thinking, Working, Attention, Happy, Idle, or Sleeping DOM visibility | PASS |

The complete observed visual order in both rounds was:

```text
Idle → Thinking → Working → Attention → Happy → Idle
```

Round 2's requested Happy deadline `+1 ms` sample still saw Happy because the real timer callback was late; the immediately following atomic poll saw Idle at `187253.1`, after the `187248.4` deadline. Similarly, some requested `deadline - 1 ms` CDP promises resumed slightly after their target while the protected state was still visible. These observations prove that runtime scheduling did not shorten a hold, but they are not represented as exact wall-clock `+1999/+2000` captures. Exact millisecond transition boundaries are supplied by the fresh 74/74 deterministic fake-clock suite; runtime evidence supplies actual application times, exact absolute-duration differences, DOM agreement, and no-early-transition evidence.

## Natural sleeping and Hook wake

The harness did not call a debug setter or controller method. It continued atomic CDP polling while the application's real local mouse-idle logic ran. After the 60000 ms inactivity rule, CDP observed:

```text
now 243751.7
logicalState sleeping
visibleState sleeping
visibleSince 243596.5
class sleeping
sleep-img display block
```

The real Python `user-prompt-submit` fixture then produced a Renderer snapshot at `243925.4`: Thinking was logical and visible, `visibleSince` was `243912.9`, its deadline was `245912.9`, the Thinking class/image were visible, and the sleeping image was hidden. This proves immediate Hook wake through the normal input path.

## Cleanup and scope limits

The acceptance Electron process was stopped with SIGINT. Both `18920` and `19222` were confirmed listener-free afterward. Final pre-documentation Git status again contained only `.superpowers/`.

This PASS is limited to the worktree runtime gate:

- `Stop -> Happy` is temporary for v1.1.0.
- Permission resolution and repeated permission reminders remain v1.2.0 work.
- There is no first-pixel acknowledgement.
- Renderer reload safely returns to Idle; persistent recovery is not part of v1.1.0.
- This run does not prove installed-package identity, signing/notarization, installation runtime, or release readiness.
