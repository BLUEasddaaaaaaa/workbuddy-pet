# Blueberry 桌面陪伴宠物 (Desktop Companion Pet) — PRD V2.0

> **历史基线说明**：本文保留 Blueberry v1.0 CodeBuddy/Windows 阶段的原始产品设计，用于追溯项目演进，不代表当前实现。当前 Codex/macOS 迭代以 [`docs/iterations/v1.1.0-codex-hooks.md`](docs/iterations/v1.1.0-codex-hooks.md) 和对应验收证据为准。
>
> **文档版本**: V2.0
> **创建日期**: 2026-05-13
> **最后更新**: 2026-05-15
> **产品状态**: V1.0 已上线，CodeBuddy 事件映射已集成

---

## 一、产品概述

### 1.1 产品定位
运行在 Windows 桌面上的像素风虚拟宠物，常驻屏幕右下角，与 CodeBuddy AI 编程助手深度联动——当 AI 在思考、编码、完成任务时，宠物会做出对应的动作反馈，让编程过程更有情感陪伴感。

### 1.2 核心价值
| 用户痛点 | 产品方案 |
|---------|---------|
| AI 执行任务时缺乏视觉反馈，不确定是否还在运行 | 宠物显示 working 动画，一眼可知 |
| 任务完成需要盯终端确认 | 宠物播放 happy 动画 + 音效，即时提醒 |
| 长时间编程缺乏情感陪伴 | 桌宠有眼球追踪、随机动作、自动睡觉等拟人行为 |
| 桌面工具太多，占注意力 | 160×160 像素透明窗口，零干扰 |

### 1.3 目标用户
- 使用 CodeBuddy 的程序员
- 喜欢虚拟宠物/桌宠的用户
- 长时间使用电脑的办公人群

---

## 二、系统架构

### 2.1 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 桌面框架 | **Electron** | 透明无边框置顶窗口 |
| 宠物身体 | **PNG 像素图** + **GIF 动图** | 各状态对应独立 GIF |
| 眼睛控制 | **SVG 叠加层** + JS | 瞳孔跟踪、眨眼动画 |
| 动画驱动 | **requestAnimationFrame** | 60fps 游戏循环 |
| 样式动画 | **CSS3 keyframes** | 各状态的呼吸/弹跳/晃动 |
| 外部通信 | **HTTP 本地服务** | 127.0.0.1:18920 |
| Hook 桥接 | **Node.js 脚本** | CodeBuddy hooks → HTTP POST |

### 2.2 项目结构

```
d:\workbuddy_pet\
├── main.js                    # Electron 主进程：窗口管理、HTTP 服务、鼠标轮询
├── preload.js                 # IPC 桥接：contextBridge 暴露 petAPI
├── package.json               # 项目配置
├── package-lock.json          # 依赖锁定
├── assets/
│   ├── mypet.png              # 宠物默认身体（idle 状态 + SVG 眼睛叠加）
│   ├── pet-sleeping.gif       # 睡眠动画
│   ├── think.gif              # 思考动画
│   ├── read.gif               # 阅读动画
│   ├── work.gif               # 工作动画
│   └── happy.gif              # 开心动画
├── hooks/
│   └── codebuddy-hook.js      # CodeBuddy Hook 脚本
└── src/renderer/
    ├── index.html             # 渲染页面：GIF 元素 + SVG 眼睛
    ├── renderer.js            # 核心逻辑：状态机、眼球追踪、眨眼、游戏循环
    └── style.css              # 各状态 CSS 动画
```

### 2.3 数据流架构

```
┌──────────────────────────────────────────────────────────────────┐
│  CodeBuddy                                                       │
│  ┌──────────┐                                                    │
│  │ Hook 事件 │  SessionStart / UserPromptSubmit / PreToolUse /   │
│  │  触发     │  PostToolUse / Stop / Notification / PreCompact   │
│  └─────┬────┘                                                    │
│        │ node hooks/codebuddy-hook.js [事件名]                    │
│        ▼                                                         │
│  ┌──────────────────┐                                            │
│  │ codebuddy-hook.js │  解析 stdin payload → 决定目标状态         │
│  │ (Hook 脚本)       │  Stop: 检查 stop_hook_active              │
│  │                   │  Notification: 检查 notification_type      │
│  └─────┬────────────┘                                            │
│        │ HTTP POST 127.0.0.1:18920/state {"state":"working"}     │
└────────┼─────────────────────────────────────────────────────────┘
         │
┌────────▼─────────────────────────────────────────────────────────┐
│  Electron 主进程 (main.js)                                        │
│  ┌────────────────┐  ┌────────────────┐  ┌───────────────────┐  │
│  │ HTTP 服务       │  │ 鼠标位置轮询    │  │ 窗口管理          │  │
│  │ :18920 /state  │  │ 16ms/次 IPC推送 │  │ 透明/置顶/无边框  │  │
│  │ :18920 /happy  │  │                │  │ 拖拽/ setPosition │  │
│  └───────┬────────┘  └───────┬────────┘  └───────────────────┘  │
│          │ IPC trigger-state   │ IPC mouse-position               │
└──────────┼────────────────────┼──────────────────────────────────┘
           │                    │
┌──────────▼────────────────────▼──────────────────────────────────┐
│  渲染进程 (renderer.js + preload.js)                              │
│                                                                   │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────────────┐ │
│  │ 状态机       │  │ 眼球追踪系统  │  │ 眨眼系统                │ │
│  │ 6种状态切换   │  │ 双眼同步追踪  │  │ 4-9s 随机触发           │ │
│  │ 优先级互斥   │  │ lerp 平滑插值 │  │ 3% 双眨概率             │ │
│  │ 超时保底     │  │              │  │ 80ms闭/120ms开          │ │
│  └──────┬──────┘  └──────────────┘  └──────────────────────────┘ │
│         │                                                         │
│  ┌──────▼──────────────────────────────────────────────────────┐ │
│  │                      渲染层                                 │ │
│  │  PNG (idle) + SVG (眼睛) + GIF (各状态) + CSS (动画)        │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

---

## 三、宠物状态机

### 3.1 状态总览

| 状态 | 触发源 | 显示资源 | CSS 动画 | 音效 | 持续时间 |
|------|--------|---------|----------|------|---------|
| **idle** | 默认 / SessionStart / PreCompact | `mypet.png` + SVG 眼睛 | 呼吸浮动 3s | 无 | 持续 |
| **thinking** | UserPromptSubmit / 待机随机 5% | `think.gif` | 上下浮动 2.5s | 无 | 5-10s 随机 |
| **working** | PreToolUse / PostToolUse | `work.gif` | 左右微晃 0.6s | 无 | 30s 超时保底 |
| **happy** | Stop / Notification(idle_prompt) | `happy.gif` | 弹跳 0.4s | 上行琶音 C5→C6 | 3s |
| **sleeping** | 60s无操作 / SessionEnd | `pet-sleeping.gif` | 睡眠呼吸脉冲 2s | 无 | 持续到鼠标移动 |
| **attention** | Notification(非idle_prompt) | `think.gif` (复用) | 脉冲缩放 1s | 无 | 5s |

### 3.2 状态优先级

```
happy (最高) > working > attention > thinking > idle
                                  sleeping (独立系统)
```

规则：
- `happy` 可打断所有状态（含 working、attention）
- `working` 可打断 thinking、attention
- `happy` 执行期间，working/attention 事件被忽略
- `sleeping` 由独立的鼠标空闲计时器控制，任何 hook 事件均可唤醒

### 3.3 状态流转图

```
                          ┌─────────────┐
                          │    idle     │◄─── SessionStart / PreCompact
                          │ mypet.png   │     恢复默认显示
                          │ +SVG 眼睛   │
                          └──────┬──────┘
                                 │
              ┌──────────────────┼──────────────────┐
              │                  │                  │
              ▼                  ▼                  ▼
      ┌──────────────┐  ┌──────────────┐   ┌──────────────┐
      │   thinking   │  │   working    │   │    happy     │
      │  think.gif   │  │  work.gif    │   │  happy.gif   │
      │ UserPrompt   │  │ PreToolUse   │   │    +音效      │
      │  /待机随机   │  │ PostToolUse  │   │ Stop(完成)   │
      └──────┬───────┘  └──────┬───────┘  │ Notification │
             │                 │          │ (idle_prompt)│
             │                 │          └──────┬───────┘
             │                 │                 │
             │    ┌────────────┼─────────────────┘
             │    │            │
             ▼    ▼            ▼
          ┌──────────────┐
          │  attention   │◄─── Notification(非idle_prompt)
          │ think.gif    │     权限请求/其他通知
          │ (复用)       │
          └──────┬───────┘
                 │
                 ▼
          所有状态 → 3s/5s/30s后 → idle

  ┌──────────────┐                ┌──────────────┐
  │   sleeping   │◄─── 60s无操作  │    任意鼠标   │
  │pet-sleeping  │     /SessionEnd│    移动/hook  │
  │   .gif       │                │    事件       │
  └──────────────┘                └──────┬───────┘
         ▲                               │
         └───────────────────────────────┘
                          wakeUp()
```

---

## 四、CodeBuddy Hook 事件映射

### 4.1 映射表

| Hook 事件 | stdin payload 检查 | 宠物状态 | 语义 |
|-----------|-------------------|---------|------|
| `SessionStart` | — | idle | 会话开始，回到待机 |
| `SessionEnd` | — | sleeping | 会话结束，进入睡眠 |
| `UserPromptSubmit` | — | thinking | 用户发送消息，AI 开始思考 |
| `PreToolUse` | — | working | AI 即将调用工具（读文件等） |
| `PostToolUse` | — | working | AI 工具调用完成，继续工作 |
| `Stop` | `stop_hook_active === false` | **happy** | AI 正常完成任务 |
| `Stop` | `stop_hook_active === true` | *(跳过)* | Hook 触发的继续运行，非真实完成 |
| `Notification` | `notification_type === "idle_prompt"` | **happy** | 任务弹窗出现，AI 等待用户 |
| `Notification` | 其他类型 | attention | 权限请求等通知 |
| `PreCompact` | — | idle | 上下文压缩前，回到待机 |

### 4.2 Hook 安装

配置文件：`~/.codebuddy/settings.json`

```json
{
  "hooks": {
    "SessionStart":     [{"type":"command","command":"node d:/workbuddy_pet/hooks/codebuddy-hook.js SessionStart"}],
    "SessionEnd":       [{"type":"command","command":"node d:/workbuddy_pet/hooks/codebuddy-hook.js SessionEnd"}],
    "UserPromptSubmit": [{"type":"command","command":"node d:/workbuddy_pet/hooks/codebuddy-hook.js UserPromptSubmit"}],
    "PreToolUse":       [{"type":"command","command":"node d:/workbuddy_pet/hooks/codebuddy-hook.js PreToolUse"}],
    "PostToolUse":      [{"type":"command","command":"node d:/workbuddy_pet/hooks/codebuddy-hook.js PostToolUse"}],
    "Stop":             [{"type":"command","command":"node d:/workbuddy_pet/hooks/codebuddy-hook.js Stop"}],
    "Notification":     [{"type":"command","command":"node d:/workbuddy_pet/hooks/codebuddy-hook.js Notification"}],
    "PreCompact":       [{"type":"command","command":"node d:/workbuddy_pet/hooks/codebuddy-hook.js PreCompact"}]
  }
}
```

### 4.3 典型使用场景流程

```
用户向 CodeBuddy 发送消息："帮我重构这个函数"
  │
  ├─ UserPromptSubmit → thinking (think.gif, AI 思考中...)
  │
  ├─ PreToolUse → working (work.gif, AI 读取文件...)
  ├─ PostToolUse → working (work.gif, 继续工作...)
  │
  ├─ PreToolUse → working (work.gif, AI 写入文件...)
  ├─ PostToolUse → working (work.gif, 继续工作...)
  │
  ├─ Stop (stop_hook_active=false) → happy (happy.gif + 音效, 任务完成!)
  │   └─ 3秒后 → idle (恢复默认)
  │
  └─ Notification (idle_prompt) → happy (弹窗出现, 确认完成)
      └─ 3秒后 → idle (恢复默认)
```

---

## 五、常驻子系统

### 5.1 眼球追踪系统

| 参数 | 值 | 说明 |
|------|-----|------|
| 跟随范围 X/Y | 8px | 瞳孔最大偏移量 |
| 跟随速度 | 0.3 | lerp 插值系数 |
| 轮询频率 | 16ms (~60fps) | 主进程 → 渲染进程 IPC |
| 双眼同步 | 统一方向向量 | 防止对眼/抽搐 |

**工作原理**：主进程通过 `screen.getCursorScreenPoint()` 获取全局鼠标位置，映射到 SVG viewBox 坐标空间，渲染进程用 lerp 平滑插值驱动 SVG `<g>` 的 `transform`。

**适用状态**：仅 idle 状态生效；其他状态隐藏 SVG 眼睛层。

### 5.2 眨眼系统

| 参数 | 值 | 说明 |
|------|-----|------|
| 触发间隔 | 4-9 秒随机 | 自然眨眼节奏 |
| 闭眼时间 | 80ms | 眼皮扫下 |
| 睁眼时间 | 120ms | 眼皮收回 |
| 双眨概率 | 3% | 快速眨两次 |
| 实现方式 | SVG `<rect>` height 动画 | rAF 驱动 |

### 5.3 待机随机动作

| 动作 | 触发概率 | 冷却时间 | 持续时间 | CSS 动画 |
|------|---------|---------|---------|----------|
| reading | 5% / 10秒检查 | 2 分钟 | 5-10s 随机 | 上下浮动 3s |
| thinking | 5% / 10秒检查 | 无 | 5-10s 随机 | 上下浮动 2.5s |

仅在 idle 且非拖拽、非睡眠时触发。

### 5.4 睡眠系统

| 参数 | 值 |
|------|-----|
| 进入条件 | 60 秒无鼠标移动 |
| 唤醒条件 | 鼠标移动 / 任何 hook 事件 |
| 显示 | `pet-sleeping.gif` + 睡眠呼吸脉冲 |

### 5.5 任务完成音效

- 触发时机：进入 happy 状态
- 实现：Web Audio API
- 音色：正弦波上行琶音 C5(523Hz) → E5(659Hz) → G5(784Hz) → C6(1047Hz)
- 每音间隔 100ms，持续时间 500ms，音量峰值 0.25

---

## 六、资源清单

### 6.1 已有资源

| 文件 | 用途 | 状态 |
|------|------|------|
| `assets/mypet.png` | idle 状态宠物身体图 (配合 SVG 眼睛) | ✅ |
| `assets/pet-sleeping.gif` | sleeping 状态动画 | ✅ |
| `assets/think.gif` | thinking 状态动画 | ✅ |
| `assets/read.gif` | reading 待机动画 | ✅ |
| `assets/work.gif` | working 状态动画 | ✅ |
| `assets/happy.gif` | happy 状态动画 | ✅ |

### 6.2 缺失/复用资源

| 文件 | 用途 | 当前状态 | 优先级 |
|------|------|---------|--------|
| `assets/attention.gif` | attention 状态动画 | **复用 think.gif** | P1 |

> `attention` 目前与 `thinking` 共用 `think.gif`，视觉上无法区分"AI在思考"和"需要用户注意"。建议后续制作独立的 attention.gif（如竖耳朵/举手/惊讶表情）。

### 6.3 资源规范

| 属性 | 要求 |
|------|------|
| 风格 | 像素风，与 `mypet.png` 一致 |
| 尺寸 | 与 `mypet.png` 相同画布尺寸 |
| 背景 | 透明 |
| 格式 | GIF，循环播放 |
| 文件名 | 全小写，连字符分隔，状态名命名 |

---

## 七、API 参考

### 7.1 HTTP 服务

**地址**: `http://127.0.0.1:18920`

#### POST /state

通用状态触发接口。

**请求**：
```json
POST /state
Content-Type: application/json

{"state": "working"}
```

**合法 state 值**：`idle` | `thinking` | `working` | `happy` | `sleeping` | `attention`

**成功响应**：
```json
{"status": "ok", "state": "working"}
```

**错误响应** (无效状态)：
```json
{"status": "error", "message": "invalid state: xxx. valid: idle,thinking,working,happy,sleeping,attention"}
```

#### POST /happy

向后兼容接口，等价于 `POST /state {"state":"happy"}`。

**成功响应**：
```json
{"status": "ok", "state": "happy"}
```

### 7.2 IPC 通道

| 通道名 | 方向 | 数据 | 说明 |
|--------|------|------|------|
| `trigger-state` | 主进程 → 渲染进程 | `string` (状态名) | 通用状态触发 |
| `trigger-happy` | 主进程 → 渲染进程 | 无 | 向后兼容 |
| `mouse-position` | 主进程 → 渲染进程 | `{cursorX, cursorY, windowX, windowY, windowWidth, windowHeight}` | 全局鼠标位置 |
| `move-window` | 渲染进程 → 主进程 | `{dx, dy}` | 窗口拖拽偏移 |
| `drag-end` | 渲染进程 → 主进程 | 无 | 拖拽结束刷新透明层 |

### 7.3 petAPI (渲染进程可用)

| 方法 | 参数 | 说明 |
|------|------|------|
| `petAPI.onTriggerState(callback)` | `callback(state: string)` | 监听状态切换 |
| `petAPI.onTriggerHappy(callback)` | `callback()` | 向后兼容 |
| `petAPI.onMousePosition(callback)` | `callback(pos: object)` | 监听鼠标位置 |
| `petAPI.moveWindow(dx, dy)` | `dx: number, dy: number` | 移动窗口 |
| `petAPI.dragEnd()` | — | 拖拽结束通知 |
| `petAPI.scale` | — | 缩放倍数 |
| `petAPI.platform` | — | 操作系统 |

---

## 八、异常处理

| 场景 | 处理策略 |
|------|---------|
| HTTP 服务不可用 | Hook 脚本 100ms 超时静默失败，不阻塞 CodeBuddy |
| GIF 文件损坏 | `work.gif` 有 error 事件监听，降级显示 PNG+SVG idle + CSS working 动画 |
| 多次 POST /happy | 重置 happyTimer 计时器，支持连续触发 |
| PreToolUse hook 未返回 allow | Hook 脚本始终返回 `{"decision":"allow"}` |
| Stop hook 无限循环 | 检查 `stop_hook_active`，为 true 时跳过 |
| Windows DWM 透明层丢失 | blur/focus/setPosition 后主动 `setBackgroundColor('#00000000')` |
| working 状态无限卡住 | 30s 超时自动回 idle |
| attention 状态无限卡住 | 5s 超时自动回 idle |

---

## 九、启动参数

| 参数 | 示例 | 说明 |
|------|------|------|
| `--scale=N` | `npm start -- --scale=2` | 缩放倍数，默认 1，窗口 = 160×N px |

---

## 十、验收标准

- [x] 宠物窗口透明无边框，始终置顶于桌面
- [x] 眼睛实时跟随鼠标，平滑自然
- [x] 随机 4-9 秒自动眨眼
- [x] 支持拖拽移动宠物位置
- [x] 60s 无操作自动进入睡眠，鼠标移动唤醒
- [x] CodeBuddy 发消息 → thinking 动画
- [x] CodeBuddy 调用工具 → working 动画
- [x] CodeBuddy 任务完成 → happy 动画 + 音效
- [x] CodeBuddy 权限通知 → attention 动画
- [x] working 状态 30s 超时保底
- [x] happy 优先级最高，可打断所有状态
- [x] Windows DWM 透明层修复
- [ ] attention 独立 GIF（当前复用 think.gif）

---

## 十一、版本规划

### V1.1 — 体验打磨
- [ ] 制作独立 `attention.gif`，区分思考与提醒
- [ ] 窗口位置记忆（关闭后重启保持位置）
- [ ] 系统托盘图标（右键菜单：显示/隐藏/退出）

### V1.2 — 丰富待机行为
- [ ] 打哈欠动画
- [ ] 伸懒腰动画
- [ ] 舔爪子动画
- [ ] 点击宠物触发反应（被摸头/跳起）

### V1.3 — 交互系统
- [ ] 喂食/喝水功能
- [ ] 连续点击 → 生气动画
- [ ] 对话气泡提示

### V1.4 — 进阶功能
- [ ] 多皮肤/装扮系统
- [ ] 定时提醒（喝水/休息）
- [ ] 心情/状态数值系统
- [ ] 云端数据同步

---

## ADR 记录

### ADR-001: 使用本地 HTTP 而非 Named Pipe 接收外部触发
- **状态**: 已采纳
- **决策**: 监听 127.0.0.1:18920，接收 POST /state
- **原因**: HTTP 比 Named Pipe 跨语言调用更简单，CORS 头设置后支持浏览器扩展，仅本地回环地址安全可控
- **代价**: 占用一个端口，需处理端口冲突

### ADR-002: 鼠标位置通过主进程 IPC 推送而非渲染进程监听
- **状态**: 已采纳
- **决策**: 主进程用 `screen.getCursorScreenPoint()` 轮询，16ms 推送一次
- **原因**: 透明置顶窗口的 mousemove 在鼠标穿透模式下无法触发
- **代价**: 增加约 60 次/秒 IPC 调用

### ADR-003: Stop/Notification 事件动态判断而非静态映射
- **状态**: 已采纳
- **决策**: Stop 检查 `stop_hook_active`，Notification 检查 `notification_type`
- **原因**: 静态映射无法区分"AI完成任务"和"Hook循环触发"，也无法区分"任务弹窗"和"权限请求"
- **代价**: Hook 脚本需要解析 stdin payload，增加了约 50ms 延迟

### ADR-004: working 状态 30s 超时保底
- **状态**: 已采纳
- **决策**: working 状态设 30s 超时自动回 idle
- **原因**: Hook 事件可能丢失（如 PostToolUse 后不再触发 Stop），导致宠物永久卡在 working
- **代价**: 正常长时间工作会被截断，但可被新的 working 事件刷新
